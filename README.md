# 휴대폰 신청 시스템 목업 V2 — 화면집

휴대폰에서 열어 보는 목업 뷰어입니다. 단일 HTML 파일 하나로 동작합니다.

- 화면 13개: 고객 신청서(모바일·PC) / 접수·개통 처리 / 상품 / 설정
- 상단 칩으로 화면 전환, 하단 컨트롤로 맞춤·확대, 확대 후 밀어서 보기
- 각 아트보드는 클래스명이 겹치므로 `iframe srcdoc` 으로 격리했습니다

검색엔진 색인은 `robots.txt` 와 `noindex` 메타로 막아 두었습니다.
링크를 아는 사람만 보는 용도입니다.
